Продолжаем работать с интернет-магазином (уроки 2-4).

1. Разбить (если еще не сделано) файл со скриптами на несколько отдельных файлов. Каждый класс должен находиться в своем файле.
2. Написать конфигурацию для вебпака (как мы это сделали на вебинаре), чтобы импорт файлов был непосредственно в этих файлах, а на index.html подключался один бандл (сгенерированный вебпаком код)
3. Результат билда сразу помещать в папку public (и server.js) должен ссылаться на нее.
Задания на *
1) Попробовать загрузчик scss (sass/less/styl). То есть перевести стили на язык препроцессора и загрузить эти стили через вебпак.
2) Попробловать добиться поведения: перед push в гитхаб автоматически запускать сборку проекта, и чтобы после пуша, в репозитории хранилась актуальная сборка (соответственно чтобы хероку автоматически читала актуальную версию)